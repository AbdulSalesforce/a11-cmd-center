{
  "name": "a11y-audit-tool",
  "version": "1.0.0",
  "description": "Salesforce Accessibility Audit Tool",
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  },
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install --include=dev",
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "heroku-postbuild": "npm run postinstall && cd client && npm run build"
  },
  "keywords": ["accessibility", "audit", "wcag", "salesforce"],
  "author": "Jonathan Bell",
  "license": "ISC"
}
